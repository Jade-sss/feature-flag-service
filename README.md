Feature Flag Service (FastAPI)

Starter scaffold for a production-ready feature-flag service using FastAPI, Postgres and Redis.

Structure:
- `src/` - application code
- `tests/` - tests
- `infra/` - deployment artifacts (docker, k8s)

Next steps: implement DB models, migrations, and evaluation engine.
